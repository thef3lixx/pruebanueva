*Base de datos*
#1 Instalar MongoDB - https://www.mongodb.com/try/download/community
#2 Dejar URL por defecto y presionar el boton "Connect" para que el motor de bdd arranque
#3 Crear una base de datos de nombre "node_crud", collection name "users" con las tablas "users" 
que incluye los campos:
    name, email y pass.
y la otra tabla "products" con los campos:
    name, description y price.
#4 Ingresar a VSC e instalar estas dependencias con los siguientes comandos:
    npm i express mongoose express-session ejs multer dotenv
    npm install -D nodemon

*Dependencias de API*
#1 Instalar la dependencia Express con el comando:
    npm install express
#2 Instalar la dependencia de Jason Web Token con el comando:
    npm install jsonwebtoken
#3 
